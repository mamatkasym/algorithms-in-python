
def create_max_number(A: list[int], B: list[int], k: int) -> list[int]:
    """
    source: https://leetcode.com/problems/create-maximum-number/
    :param A: list of digits
    :param B: list of digits
    :param k: length of the desired array
    :return: list of digits of length k <= len(A)+len(B) such that the number, from which digits, is maximal.
    """
